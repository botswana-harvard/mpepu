use the lists.ods spreadsheet to help generated the initial insert statements
for the "list tables"

copy and past the statements into "lists.sql"

run the insert statements:

cd ~/bhp056/mpepu/initial_data
python upload_data.py -u root -p -f lists.sql

